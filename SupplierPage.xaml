<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppMobile.SupplierPage"
             Title="SupplierPage">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Enter supplier name" Margin="20" Text="{Binding SupplierName}" HeightRequest="50" />
            <Editor Placeholder="Enter supplier adress" Margin="20" Text="{Binding Adress}" HeightRequest="50" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Save Supplier" Grid.Column="0" MaximumWidthRequest="200" Clicked="OnSaveButtonClicked" />
                <Button Grid.Column="1" Text="Delete" Clicked="OnDeleteButtonClicked"/>
                <!--<Button Text="Show on Map" Grid.Column="1" MaximumWidthRequest="200" Clicked="OnShowMapButtonClicked"></Button>-->            
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>